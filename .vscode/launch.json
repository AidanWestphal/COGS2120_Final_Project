{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/npuzzle_char.out",
        "args": [],
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "build",
    }
    ]
}